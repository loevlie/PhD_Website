Django==5.0.1
gunicorn==21.2.0
whitenoise==6.6.0
markdown==3.7
Pygments==2.18.0
python-frontmatter==1.1.0
psycopg[binary]>=3.1.18
django-taggit==6.1.0
dj-database-url==2.3.0
# Claude proxy for the "Ask this post" reader chat. Only called from
# portfolio/views/ask.py; the endpoint returns HTTP 503 when the
# ANTHROPIC_API_KEY env var is unset, so missing credentials don't
# crash views — the import is cheap and safe regardless.
anthropic>=0.46

# Editor spell-check (staff-only). Pure-Python, ships its own English
# corpus. Called from portfolio/views/editor_assist.py on a debounce
# from the editor JS. Zero runtime impact on reader-facing pages.
pyspellchecker>=0.8

# HTML walker used by the per-post notation auto-wrap (render pipeline
# scans post bodies for Post.notation terms and wraps them in hover
# pills). Called from portfolio/blog/__init__.py :: _wrap_notation_terms.
beautifulsoup4>=4.12

# Cloudflare R2 object storage for user uploads (avatars, blog cover
# images, editor body images). R2 speaks the S3 API; django-storages'
# S3 backend routes Django file operations to it. Activated only when
# R2_BUCKET_NAME is set in the environment — local dev stays on disk.
django-storages[s3]>=1.14

# Default scientific-Python stack available to /pyfig blocks. Each post
# can also declare extras via a per-post deps file (see _render_pyfig).
matplotlib>=3.9
numpy>=1.26
scipy>=1.13
pandas>=2.2
scikit-learn>=1.5
seaborn>=0.13
