Django==5.0.1
gunicorn==21.2.0
whitenoise==6.6.0
markdown==3.7
Pygments==2.18.0
python-frontmatter==1.1.0
psycopg[binary]>=3.1.18
django-taggit==6.1.0
dj-database-url==2.3.0
# matplotlib + numpy for the /pyfig slash command — server-rendered
# figures via subprocess (cached by content hash). Headless backend
# (Agg) is set by the wrapper so no display is needed.
matplotlib>=3.9
numpy>=1.26
